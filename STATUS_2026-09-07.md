# DSRL offline-to-online "dip" 연구 — 진행상황 공유

**2026-09-07 기준 · 팀 공유용** (AUC 열은 9/7에 통일한 정의로 다시 계산한 값. 그림·`metrics.csv`: `scripts/plot_results.py`, 커밋 90975b4)
저장소: `msp0617/dsrl` 브랜치 `o2o` · 전체 기록: `HANDOFF.md`, 요약: `HANDOFF_CHAT.md`

---

## 요약

- **무엇을**: DSRL(Wagenmaker et al., CoRL 2025)을 robomimic Can(보조 Square)에서 offline-to-online으로 학습할 때, 초반 수만 step 동안 성공률이 사전학습 정책보다 **떨어지는 구간(dip)** 의 원인을 밝히고 처방을 비교한다.
- **어디까지**: 계획한 **90 run 전부 완료(9/6)**. 실험 단계는 끝났고 포스터 작성 중(**9/9 제출**). 후속 실험은 포스터 뒤 Google Cloud에서.
- **핵심 결과**: dip의 정체는 **SAC auto-α(목표 엔트로피 0)가 학습 시작 15k step 안에 정책 엔트로피를 17 → 0으로 무너뜨리는 구간**이다. 엔트로피가 10 아래로 떨어지지 않게 하면 **Can·Square 모두 dip이 사라진다.** 단, Square에서는 엔트로피를 붙드는 데 필요한 α가 critic 타깃(soft backup의 엔트로피 보너스)까지 부풀려 후반 성능을 잃는다. 다음 실험은 이 상충을 푸는 것이다.
- **처음 가설의 운명**: "critic 초기화(Q_W가 무작위)가 원인"이라는 출발 가설은 **부분적으로만 맞았다**. critic 사전학습은 dip을 얕게 하지만 시점과 최종 성능은 못 바꾼다. 후속으로 세운 "Q 스케일이 방아쇠" 가설과 그 위의 게이트 설계는 **기각**.

---

## 1. 배경

- **DSRL-NA**: 학습이 끝난 diffusion policy π_dp를 고정하고, π_dp에 넣을 초기 노이즈 w ∈ [−1, 1]^28(4스텝 × 7차원)을 고르는 SAC actor π_W(tanh-Gaussian)를 학습한다. critic은 둘 — Q_A(행동 공간, 환경 보상으로 학습)와 Q_W(노이즈 공간, Q_A에서 증류).
- **기준선**: π_dp에 N(0, I) 노이즈를 넣은 성공률 **0.405 ± 0.014**(Can, 500 에피소드 × 3). 무작위 π_W를 붙인 step-0 값(0.5~0.7)은 이보다 높지만 seed마다 흔들리므로 회복 기준은 0.405로 잡았다.
- **문제**: 논문 기본 설정(baseline)으로 온라인 학습을 시작하면 성공률이 **0.24(39k step)까지 떨어지고 84k step에야 0.405를 회복**한다. 실제 로봇이라면 사전학습 정책보다 나쁜 정책을 수만 번 굴리는 구간이다.

## 2. 설정

- **과제**: robomimic Can(주), Square(일반성 확인). 희소 보상. γ = 0.99(Can) / 0.999(Square).
- **예산**: 초기 rollout 24,016 env step(Square 32,016) 뒤 학습, env step 1당 grad step 1.25. 대부분 150k(초기 조건 일부 300k). 평가는 100 에피소드, 5k step마다(100k 이후 25k). 마지막 평가 Can 129k, Square 127k.
- **조건 축**
  - **A. critic 초기화**: baseline(무작위) / iql(IQL로 Q_A 사전학습 → Q_W 증류) / warmup(DSRL 자체 업데이트로 사전학습, actor까지 로드) / warmupc(같은 것, critic만 로드)
  - **B. 리플레이의 데모 비율**: prefill(데모를 버퍼에 한 번 넣음; 상류 `load_offline_data` 옵션, 공개 config 기본 False; 자연 감쇠 0.91 → 0.43) / fixed 0.5(RLPD식 대칭 샘플링) / linear 0.8 → 0.1
  - **C. 온도·엔트로피**: 고정 α ∈ {0.01, 0.03, 0.1, 0.3, 1.0} / auto-α + 목표 엔트로피 12 또는 6 / auto-α 초기값 0.3 / α 학습률 2배·절반
  - **D. critic 타깃**: 보상 ×0.25·×2 / 엔트로피 보너스 제거(hard backup)
- **지표**: seed 평균곡선의 **최저값**(공통 5k 격자), 기준선 0.405 **회복 시점**, **early AUC**(초기 평가를 online step 0에 두고 online 0~76k(Can) / 0~68k(Square) = env 100k까지를 seed별 적분 후 평균), **100k·129k 성공률**. 평가 노이즈가 ±0.1 수준이라 개별 seed의 한 점으로는 말하지 않고 3~5 seed 평균으로 본다.

## 3. 결과

### 3.1 Can — 축 A(critic 초기화)·B(데모 리플레이)

| 조건 | n | 최저 | 회복 | AUC | 100k | 129k |
|---|---|---|---|---|---|---|
| baseline | 5 | 0.24 @39k | 84k | 0.41 | 0.48 | 0.53 |
| iql | 5 | 0.34 | 73k | 0.49 | 0.54 | 0.56 |
| warmupc(critic만) | 3 | 0.32 | 42k | 0.51 | — | 0.61 |
| warmup(actor까지) | 3 | step 0 **0.02** | 61k | 0.48 | — | 0.54 |
| mix_prefill(데모 prefill; 상류 옵션, 공개 config 기본 False) | 3 | 0.09 @34k | **47k** | **0.68** | **0.84** | **0.86** |
| mix_fixed 0.5 | 3 | 0.29 | 44k | 0.67 | — | 0.84 |
| mix_linear 0.8→0.1 | 3 | 0.02 | 51k | 0.64 | — | 0.75 |
| iql + prefill | 3 | 0.16 | 52k | 0.60 | — | 0.85 |

- critic 사전학습(iql, warmupc)은 dip을 얕게 하지만(0.24 → 0.32~0.34) 시점과 최종은 그대로. 사전학습한 **actor**까지 로드하면 step 0에서 붕괴(0.02).
- 데모를 리플레이에 유지하는 것이 가장 큰 지렛대(회복 84k → 47k, 최종 0.86). 명시적 linear 스케줄(0.64)과 두 축의 교차(iql+prefill 0.60)는 논문의 자연 감쇠(0.68)보다 낫지 않다(차이 0.04~0.08, SE 0.01~0.02).

### 3.2 Can — α·엔트로피 축 (헤드라인)

| 조건 | n | 최저 | AUC | 100k | 129k |
|---|---|---|---|---|---|
| auto-α, 목표 엔트로피 0 (= baseline) | 5 | 0.24 | 0.41 | 0.48 | 0.53 |
| 고정 α 0.01 | 3 | 0.00 | 0.15 | 0.23 | 0.30 |
| 고정 α 0.03 | 3 | 0.00 | 0.23 | 0.37 | 0.41 |
| 고정 α 0.1 | 3 | 0.14 | 0.50 | 0.63 | 0.62 |
| **고정 α 0.3** | **5** | **0.47 (dip 없음)** | **0.66** | **0.79** | **0.74** |
| 고정 α 1.0 | 3 | 0.03 | 0.27 | 0.26 | 0.15 |
| auto-α, 목표 12 (tent12) | 3 | 0.33 | 0.57 | 0.70 | 0.69 |
| **auto-α, 목표 12, 초기 0.3 (tent12i)** | 3 | **0.46 (dip 없음)** | 0.59 | 0.68 | 0.70 |
| auto-α, 목표 6 (tent6, 100k) | 3 | 0.22 @29k → 34k에 0.45 | 0.51 | 0.61 | — |
| 보상 ×0.25 / ×2 | 3/3 | 0.08 / 0.23 | 0.32 / 0.44 | 0.38 / 0.55 | — |
| hard backup(엔트로피 보너스 제거) | 3 | 0.10 | 0.30 | 0.31 | — |

- 고정 α = 0.3 또는 "목표 엔트로피 12 + 초기 α 0.3"이면 평균곡선이 기준선 0.405 아래로 내려가지 않는다(**dip 없음**). 데모 없이도 AUC가 데모 리플레이(0.68)와 같은 수준.
- α 스윕은 U자: 너무 낮으면(0.01~0.03) 엔트로피가 0으로 붕괴해 성공률 0, 너무 높으면(1.0) 학습이 안 된다.

### 3.3 Square (rollout 32k, 마지막 평가 127k)

| 조건 | n | 42k(dip 시점) | AUC online 0~68k | 100k | 127k |
|---|---|---|---|---|---|
| baseline | 3 | **0.21** | 0.40 | 0.44 | 0.47 |
| iql | 3 | 0.50 | 0.44 | 0.48 | 0.56 |
| mix_prefill | 3 | 0.03 | 0.43 | 0.63 | 0.62 |
| 고정 α 0.3 | 5 | 0.36 | 0.41 | 0.40 | 0.36 (seed 0.09~0.60) |
| auto-α, 목표 12 | 3 | **0.41 (dip 없음)** | 0.43 | 0.34 | 0.40 |
| auto-α, 목표 6 | 3 | 0.28 | 0.38 | 0.30 | 0.29 |

- Can에서 예측한 대로 baseline의 dip이 42k에 재현됐다. 목표 엔트로피 12는 dip을 없앤다.
- 그러나 **엔트로피를 붙든 조건은 모두 127k에서 baseline(0.47) 아래**. 고정 0.3은 5 seed 전부 엔트로피가 0 아래로 다시 무너졌고 seed 편차가 매우 크다(0.09~0.60). Square에서는 "이득 없음"까지만 말할 수 있다.

### 3.4 용량-반응 — 두 과제에서 같다

| 과도기 엔트로피 바닥 | Can: 최저 → 기준선 회복 | Square: 최저 → 회복 |
|---|---|---|
| 0 (auto-α, 목표 0) | 0.24 → 84k | 0.21 → 62k |
| ~6 (tent6 / 고정 0.1) | 0.22 → 34k (고정 0.1: 0.14 → 39k) | 0.28 → 52k |
| ~11 (고정 0.3, tent12i / tent12) | 0.47, dip 없음 | 0.41, dip 없음 |

dip은 엔트로피가 **떨어지는 동안** 생기고, 떨어지기를 멈추면 곧 회복된다. 바닥이 6이면 깊이는 비슷하되 짧고, 11 이상이면 없다.

## 4. 해석

1. **dip = auto-α의 엔트로피 붕괴.** 목표 엔트로피 0인 auto-α는 학습 시작 15k step 안에 정책 엔트로피를 17 → 0으로 무너뜨린다(`logp_mean` −17@26k → −6@30k → 0@40k, actor 평균의 크기 |μ| 0.2 → 0.8). 모든 auto-α 조건의 바닥이 그 창(Can 35k 전후, Square 42k)에 있다. Q_W가 아직 부정확한데 actor가 한 점으로 몰리는 순간이다.
2. **critic 쪽 설명은 시점을 못 움직였다.** critic 사전학습은 dip을 얕게만 한다. 보상을 ×0.25~×2로 바꿔도(Q 스케일 8배 범위) 첫 하락 시점이 격자 한 칸(2.5k) 안. hard backup은 오히려 더 깊다. α 감쇠를 2배로 하면 5k 앞당겨지지만 절반으로 해도 늦춰지지 않는다.
3. **Can에서는 처방이 성립한다.** 고정 α 0.3 또는 목표 엔트로피 12 + 초기 α 0.3이면 dip 없이 100k에서 0.68~0.79(baseline 0.48).
4. **Square에서는 dip 제거와 최종 성능이 상충한다.** 원인은 진단 로그로 확인됐다: 엔트로피를 붙들면 α가 계속 올라가고(tent6 0.2 → 1.0~1.8, tent12 0.4 → 15~17), 그 α가 critic 타깃의 엔트로피 보너스 α·log π′에 들어가 Q_W가 폭주한다(`qw_mean` baseline 300~600 vs tent6 1,700~2,800, tent12 23,000~24,000). Square는 γ = 0.999라 보너스가 Can(0.99)보다 10배 길게 누적된다. Can은 Q가 작고 α ≤ 0.5라 드러나지 않았다.
5. **auto-α의 과도기**: auto-α는 오차 크기와 무관하게 log α를 고정 속도로 움직이므로 초기 1.0에서 내려오는 동안 undershoot가 생긴다(tent12의 34k dip 0.33). 초기값을 0.3으로 두면 사라진다(tent12i 0.46).
6. **결론 문장(초안)**: "DSRL의 O2O dip은 SAC auto-α가 목표 엔트로피 0을 향해 정책을 무너뜨리는 15k 창이다. 엔트로피가 10 아래로 떨어지지 않게 하면 dip은 두 과제에서 사라진다. 그러나 엔트로피를 붙드는 데 필요한 α가 soft backup을 통해 critic 타깃까지 부풀리므로 γ가 큰 과제(Square)에서는 후반에 손해다."

## 5. 기각된 것 (숨기지 않음)

- **Q 스케일 가설**: "Q_W의 크기(오프셋)가 dip의 방아쇠"라는 가설로 보상 배율·hard backup 9 run을 돌렸으나 첫 하락 시점이 움직이지 않았다. 기울기 비 `ratio_ge_gq`가 첫 하락 순간 0.48로 일정했던 것은 방아쇠가 아니라 auto-α 평형의 결과였다.
- **게이트**(critic이 신뢰될 때까지 타깃 지렛대를 붙드는 설계): 전제가 기각되고 작동기(hard backup)가 해로워 폐기. 코드는 남아 있으나 기본 off.

## 6. 한계와 표현 주의

- 평가 노이즈 ±0.1. 모든 주장은 3~5 seed 평균이며 SE를 함께 적는다. Square 127k는 seed 편차가 특히 크다.
- **"α = 0.3이 답"이 아니다.** Square에서 틀렸다. 주장은 "엔트로피 붕괴가 dip이고, 붕괴를 막으면 dip이 없다(두 과제). 최종 성능에 맞는 엔트로피 수준은 과제와 시기에 따라 다르다"까지.
- §4.4의 "α 상승 → Q_W 폭주"는 진단으로 확인된 사실이지만, "그래서 후반 정책이 무너진다"는 인과의 마지막 고리는 아직 실험으로 닫지 않았다(§7 ①).
- 선행 연구 조사는 완전하지 않다. "우리가 아는 한"을 붙인다.

## 7. 다음 단계

1. **포스터**(9/9 제출, 6패널): 문제 → 정체(α·엔트로피 곡선) → 축 A → 축 B → α 스윕 + 적응형 → Square 상충과 이유.
2. **후속 실험(포스터 뒤, Google Cloud)** — 우선순위 순
   - ① **actor의 엔트로피 목표는 유지하고 critic 타깃의 엔트로피 보너스는 제거**(`train.target_ent=12 train.critic_entropy_scale=0`), Square·Can 각 3 seed. §4.4가 맞으면 Square에서 dip 없이 후반도 baseline 이상이어야 한다. 이미 구현돼 있어 run만 띄우면 된다.
   - ② 목표 엔트로피 스케줄(초기 12 → 후반 0). 낮추는 시점을 무엇으로 정하느냐가 열린 문제.
   - ③ Lift·Transport로 과제 확장, seed 확장.

## 8. 관련 연구 (9/6 조사, 완전하지 않음)

- **DSRL**(arXiv 2506.15799): dip·온도 논의 없음. **LP-DS**(arXiv 2606.01151): DSRL의 노이즈가 prior 저밀도로 흘러가는 mode collapse를 섭동 크기에 대한 trust region으로 막음 — 같은 현상을 다른 지렛대로 막은 연구, **필수 인용**. 우리는 그 집중을 SAC 온도가 만든다는 것을 보였다.
- SAC Flow(2509.25756), Latent Policy Steering(2603.05296): 같은 계열, 목표 엔트로피 0 그대로 사용, dip 논의 없음.
- 일반 O2O: WSRL(2412.07762), PORL(2505.16856), OCR(2412.18855), Wang·White·White(2505.00913). 엔트로피 붕괴: TES-SAC(2112.02852), Meta-SAC(2007.01932).

## 9. 산출물

- 코드: GitHub `msp0617/dsrl` 브랜치 `o2o`. 상류(ajwagen/dsrl)와의 차이는 `O2O.md`. 모든 조건은 Hydra override 한 줄(`variant`, `offline_mix.mode`, `train.ent_coef`, `train.target_ent`, `train.reward_scale`, `train.critic_entropy_scale`).
- 결과: run별 `eval_log.csv`(성공률), `train_log.csv`(α, `logp_mean`, `qw_mean` 등 진단). 분석 스크립트 `scripts/plot_results.py`(축별 그림 + `metrics.csv`), `scripts/alpha_timing.py`.
- 기록: `HANDOFF.md`(0~19절, 시간순 전체), `HANDOFF_CHAT.md`(자급자족 요약).
