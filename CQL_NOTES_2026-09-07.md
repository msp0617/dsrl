# CQL / Cal-QL critic 사전학습 — 두 번의 설계 실패와 현재 설계 (채팅 AI 상의용, 2026-09-07 17:50 KST)

이 문서는 저장소를 못 보는 채팅 AI가 **"우리 설정에서 CQL/Cal-QL을 어떻게 넣는 게 맞는가"** 를 같이 판단하도록 쓴 것이다. 숫자는 전부 실제 로그.

## 0. 설정 (한 번에)
- DSRL-NA (Wagenmaker et al. 2025): 고정 diffusion policy π_dp, 노이즈 w ∈ [−1.5, 1.5]^28(4스텝 × 7차원)을 고르는 SAC actor π_W. critic 둘: **Q_A(s, a)** 행동 공간(환경 보상으로 학습), **Q_W(s, w)** 노이즈 공간(Q_A에서 증류: `Q_W(s,w) ← Q_A(s, π_dp(s,w))`, w ~ N(0,I)).
- 과제 robomimic Can(보조 Square). 오프라인 데이터 = robomimic MH 시연 300개를 4스텝 청크로(61,856 청크, stride 1). 청크 보상 = 4스텝 보상 합 − 4 ∈ [−4, 0](성공 전 스텝은 −1). **데이터에 w 라벨은 없다**(시연은 π_dp 없이 만들어짐).
- 할인 γ = 0.99 **청크당**(Square 0.999). 시연 return-to-go G ∈ [−371, 0], 평균 −102 (Can). IQL로 미리 배운 Q_A 평균 ≈ −100 (즉 수익 눈금과 일치).
- 온라인: SAC soft critic 타깃 `r + γ(Q̄ − α·log π′)`, auto-α **초기 1.0, 목표 엔트로피 0**. 초기 정책 엔트로피 ≈ 17 → 타깃에 청크당 +18이 더해져 학습 시작 25k step 안에 Q_W가 +55~+175 오프셋으로 덮인다(critic 초기화와 무관). 90 run의 결론: 초기 성능 dip(0.5 → 0.24 @39k, 84k에 회복)은 auto-α의 엔트로피 붕괴(17 → 0)가 원인; critic 사전학습(IQL·DSRL 자체 warmup)은 dip을 얕게만 하고(0.24 → 0.33) 시점·최종은 못 바꿈.
- 동료 연구(자체 2D 과제, w 8차원, G ∈ [0,1]): Q_A를 ② plain TD / ③ CQL(α=1, 벌점 α(E[Q(s,a_안해봄)] − E[Q(s,a_데이터)]), a_안해봄 = π_dp(무작위 노이즈)) / ④ Cal-QL(max(Q, G_t) 바닥)로 사전학습. T50: DSRL 2,160, TD 2,099, CQL 5,974(눈금 붕괴 −9), Cal-QL 793. 우리는 이 사다리를 Can에서 재현하려는 것.

## 1. 목표: 축 A(critic 초기화)를 baseline / iql / cql / calql로
통제: 온라인은 `offline_mix.mode=none`, `load_offline_data=False`(데모를 리플레이에 안 넣음), actor·α 로드 없음, cql/calql은 같은 타깃·같은 α·같은 표본, 바닥 유무만 차이. 사전학습 뒤 Q_W 증류 25k step(iql과 동일). 사전학습에는 시뮬레이터가 없고 데이터·π_dp만 있다(actor 없음 — 여기가 표준 CQL과 다른 핵심).

## 2. 시도 1 — 실패: 벌점이 부트스트랩에 실려 발산
- 타깃 `r + γ Q̄_A(s′, π_dp(s′, w′))`, w′ ~ N(0,I) (prior-noise 정책의 FQE). 벌점 `α(mean_k Q_A(s, π_dp(s,w_k)) − Q_A(s, a_data))`, k = 4, α = 5.
- step 37k(Can): cql `q_data −9,534 / −9,341 / −9,239`, `q_ood −9,700`; calql `q_data −240`, `q_ood −247`, 바닥 적용률 0.99~1.0. Square(α=25): cql −3,400.
- 진단: 벌점이 끌어내리는 행동 분포(π_dp∘prior)가 **타깃의 부트스트랩 분포와 같다.** 매 스텝 벌점이 타깃에 들어가 1/(1−γ)=100배로 증폭. 표준 CQL은 actor가 Q를 최대화해 이 쪽을 떠받치는데 우리는 actor가 없다.

## 3. 시도 2 — 부분 실패: 타깃은 분리했지만 벌점이 자기 제한이 안 됨
- 타깃을 IQL의 in-sample V(s′)로: `V ← expectile(Q̄_A(s, a_data))`, `Q_A ← r + γV(s′)`. 벌점은 그대로 평균형.
- step 30k(Can, α=5): cql `q_data +194 / +141 / +102`, `q_ood −6,764 / −5,574 / −8,257`, 벌점 −57k~−84k. calql `q_data +40~+42`, `q_ood −368~−386`, 바닥 0.99~1.0. Square(α=25): cql `q_data +94~+197`, `q_ood −306~−359`; calql `q_data +408~+475`, `v +464`, `q_ood +21~+33`, 바닥 0.37.
- 진단: 평균형 벌점의 기울기가 격차와 무관하게 상수 α. OOD Q는 TD 손실에 안 나오므로 끝없이 내려가고, Q_data는 TD 고정점에서 α/(1−γ)만큼 위로: `(r + α)/(1−γ) = (−4 + 5)/0.01 = +100` — Can 관측(+102~+194)과 일치. Square는 α/(1−γ) = 25/0.001 = 25,000까지 밀릴 수 있어 +460이 됐다.

## 4. 시도 3 — 지금 돌고 있는 것 (17:35 시작, 첫 로그 대기)
- 타깃: 시도 2와 같은 IQL V 타깃(벌점이 타깃에 못 들어감).
- 벌점: **CQL(H) log-sum-exp에 데이터 행동을 포함**: `α · ( logsumexp{ Q_A(s, π_dp(s,w_1..4)), Q_A(s, a_data) } − Q_A(s, a_data) )`, 온도 1. 항상 ≥ 0. OOD 행동은 데이터 행동보다 높을 때만 밀리고(softmax 가중치), 데이터 행동은 자기가 최대가 아닐 때만 밀려 올라간다.
- Cal-QL: 집합 안의 OOD 항을 `max(Q_A, G)`로. G > Q_data면 Q_data가 G까지 끌려 올라감(= 교정), G 아래 OOD 항은 기울기 0.
- `cql_alpha` 5(Can)/25(Square), 표본 4개/상태, 노이즈 N(0,I) 클립 없음(증류와 같은 분포), `cql_alpha=0`이면 iql과 동일.
- 기대: Can `q_data ≈ −100`, cql `q_ood`는 그보다 수십 아래에서 멈춤, calql 바닥 적용률 0.3~0.8.

## 5. 상의하고 싶은 것
1. **actor 없는 CQL의 올바른 형태.** 참조 구현(CQL, Cal-QL JAX)은 logsumexp 집합에 {균등 무작위 행동, 현재 policy 행동, 다음 상태 policy 행동}을 importance weight로 넣고 `cql_temp`, `min_q_weight` 5~10, 선택적으로 Lagrange α(목표 gap)를 쓴다. policy가 없을 때 데이터 행동을 집합에 넣어 최대값 앵커로 삼는 것(시도 3)이 타당한 대체인가, 아니면 다른 표준(예: 균등 행동을 집합에 추가, Lagrange 버전)이 필요한가?
2. **증류와의 결합.** 벌점이 누르는 "안 해 본 행동" = π_dp(s, w~N(0,I)) = **Q_W 증류가 읽는 바로 그 행동들.** 그러면 CQL은 Q_W 지형을 낮추고/평평하게 만들 뿐 순위 정보를 더해 주지 않는다. 벌점 분포를 더 넓게(±1.5 상자 균등, 또는 초기 actor 분포 1.5·tanh(N(0,1)))로 잡아야 하나? Cal-QL 바닥이 이 평평해짐을 막아 주나?
3. **soft-Q 교란.** 온라인 α=1 시작 → 청크당 +18 보너스 → 25k 안에 오프라인 눈금이 덮인다(IQL Q_W −145 → +70 실측). 교정이 살아남는지 보려고 `calql + (auto-α 초기 0.3, 목표 엔트로피 12)`(t12i)와 `calql + 데모 리플레이(prefill)`도 오늘 밤 돌린다. 이 외에 교정을 지키는 표준 방법이 있나(예: critic 타깃에서 엔트로피 보너스 제거 β=0은 구현돼 있음)?
4. **② 칸.** 지금 우리 "그냥 사전학습"은 IQL(in-sample, 과대평가 없음)이라 동료의 plain TD(+0.72 과대평가)와 다르다. 시도 1의 타깃에서 벌점만 뺀 것(prior 정책 FQE)이 정직한 plain TD인데, 이걸 별도 칸으로 넣어야 사다리가 동료와 맞는가?
5. **α 눈금.** logsumexp 형태에서 α는 속도만 정하는가, 아니면 Square(γ=0.999, Q 300~700)에서 25가 여전히 과한가?
6. **결과 해석 선.** 예상: iql ≈ calql(둘 다 dip 얕게·시점 불변), cql ≤ iql. 이 경우 "Can·28차원·auto-α에서는 critic 교정보다 온도가 지배한다"라고 쓰려는데, 이 주장을 반증하려면 어떤 결과가 나와야 하나?

## 6. 참고 수치
| 항목 | 값 |
|---|---|
| Can baseline / iql / warmupc (n=5/5/3) | 최저 0.24/0.34/0.33, early AUC(online 0~76k) 0.41/0.49/0.51, 129k 0.53/0.56/0.61, T80 1/5 도달(180k) / 없음 / 없음 |
| Can mix_prefill / iql_prefill | AUC 0.68 / 0.60, 129k 0.86 / 0.85 |
| 고정 α 0.3 / tent12i (dip 없음) | AUC 0.66 / 0.59, 129k 0.74 / 0.70 |
| Square baseline / iql | 42k 0.21 / 0.37, AUC(0~68k) 0.40 / 0.44, 127k 0.47 / 0.56 |
| 사전학습 비용 | IQL step 15 ms(Can)·24 ms(Square); π_dp 1회(배치 256) ≈ 10~15 ms(Can, DDIM 20)·40 ms(Square, DDIM 100) |
| 로그 열 | `q_mean`(데이터 행동), `v_mean`, `q_ood_mean`(표본 행동), `penalty`, `floored_frac`(바닥이 대신한 표본 비율), `return_mean` |

코드: `offline_pretrain.py`의 `run_cql`, `conservative_penalty`, `prior_noise`, `diffusion_actions`; 데이터 `make_offline_chunks.py --gamma`(returns). 커밋 15a36ea/43bb091.
