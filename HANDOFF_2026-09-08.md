# DSRL offline-to-online 프로젝트 — 현재 인수인계

> **9/8 결과 재분석 완료:** 현재 수치·해석은 `results/2026-09-08/README.md`와
> `results/2026-09-08/ALL_RESULTS.md`를 먼저 읽는다. 121개 run / 37개 조건을
> 원본 CSV에서 재집계하고 신규 18개 사전학습 로그도 확인했다.
> 아래 08:20 기록의 “신규 24개 미집계”, “Square dip 제거”, “집계부터 해야 함”은
> 역사적 상태다. 특히 Square 기준 정책 0.494에 대해 dip은 **완화**이지 제거가 아니다.
> Cal-QL의 일관된 추가 우위는 없고, hq는 tent12 대비 후반 개선 증거지만
> 초기 dip까지 해결하지는 못했다. 학습 완료 121/121 및 추가 학습 보류는 그대로다.
> 포스터 파일 자체의 그림·문구 교체는 아직 하지 않았다.

**기준 시각: 2026-09-08 08:20 KST**  
**저장소:** `https://github.com/msp0617/dsrl`, 브랜치 `o2o`  
**Drive 원본:** `/content/drive/MyDrive/dsrl_project`

이 문서가 현재 상태의 기준이다. 시간순 상세 기록은 `HANDOFF.md`, 9/7 이전 결과와
포스터 문안은 `HANDOFF_CHAT.md`와 `STATUS_2026-09-07.md`를 참고한다.

## 1. 지금 상태

- 포스터/본 분석용 **온라인 run 121/121 완료**: Can 85, Square 36.
- 유효한 오프라인 사전학습 artifact **32/32 완료**: Can 17, Square 15.
- 진행 중 0, 중단된 계획 run 0, 새로 띄워야 할 계획 run 0.
- 9/7~9/8 신규 축 24개도 모두 `[done] 150000 env steps` 확인.
- Square 신규 9개는 처음 100k까지 실행한 뒤 같은 `exp_id`로 150k까지 연장했다.
  모두 `env_steps=127136` 평가와 150k 체크포인트가 있다.
- `square_td_s3`, `square_cql_s1`은 150k 체크포인트 저장 뒤 `[done]` 출력 전에 VM이
  멈췄지만, 9/8 아침 같은 명령으로 0-step resume하여 `[done] 150000`을 기록했다.
- **지금은 신규 학습을 더 돌리지 않는다.** 우선 신규 24개 결과 집계, 그림 갱신,
  포스터 수치 검증과 문안 작성에 집중한다.

### 숫자가 헷갈릴 때

- 9/6까지 완료: 90 run.
- 9/7 Square 후속/seed 확장: 7 run.
- 9/7~9/8 TD/CQL/Cal-QL 축: 24 run.
- 합계: **121 scientific online runs**.
- 현재 검사기가 보는 온라인형 디렉터리는 대략 127개다. 본 실험 121개 외에
  `smoke_none`, `smoke_mix`, `smoke_scale`, `smoke_gate` 4개와 utility run
  `smoke_resume`, `tput_can` 2개가 있다. `robomimic-dsrl`은 TensorBoard 컨테이너라
  최신 검사기에서는 run으로 세지 않는다.
- Drive의 pretrain `.pt` 34개 중 본 실험용은 32개이며 `test_warmup.pt`,
  `test_iql.pt`는 smoke artifact다.

## 2. 완료된 온라인 행렬

### Can: 85 run

| 축 | `exp_id` 패턴 | 조건 | seed / n | 예산 |
|---|---|---|---:|---:|
| 기본 | `can_baseline_s*` | 무작위 critic | 1–5 / 5 | s1–3 300k, s4–5 150k |
| critic | `can_iql_s*` | IQL critic | 1–5 / 5 | s1–3 300k, s4–5 150k |
| critic | `can_warmup_s*` | warmup actor+critic | 1–3 / 3 | 300k |
| critic | `can_warmupc_s*` | warmup critic만 | 1–3 / 3 | 150k |
| replay | `can_mix_prefill_s*` | 데모 prefill | 1–3 / 3 | 200k |
| replay | `can_mix_fixed_s*` | 데모 비율 0.5 | 1–3 / 3 | 200k |
| replay | `can_mix_linear_s*` | 데모 비율 0.8→0.1 | 1–3 / 3 | 200k |
| 교차 | `can_iql_prefill_s*` | IQL + prefill | 1–3 / 3 | 200k |
| 파일럿 | `can_iql_linear_s1` | IQL + linear mix | 1 / 1 | 200k |
| α | `can_fixalpha_s*` | 고정 α=0.01 | 1–3 / 3 | s1 100k, s2–3 150k |
| α | `can_fixalpha_003_s*` | 고정 α=0.03 | 1–3 / 3 | 150k |
| α | `can_fixalpha_01_s*` | 고정 α=0.1 | 1–3 / 3 | 150k |
| α | `can_fixalpha_03_s*` | 고정 α=0.3 | 1–5 / 5 | 150k |
| α | `can_fixalpha_1_s*` | 고정 α=1.0 | 1–3 / 3 | 150k |
| entropy | `can_tent12_s*` | auto-α, 목표 12 | 1–3 / 3 | 150k |
| entropy | `can_tent12i_s*` | 목표 12, 초기 α=0.3 | 1–3 / 3 | 150k |
| entropy | `can_tent6_s*` | auto-α, 목표 6 | 1–3 / 3 | 100k |
| α 속도 | `can_alr_half_s*` | α lr 절반 | 1–3 / 3 | 150k |
| α 속도 | `can_alr_double_s*` | α lr 2배 | 1–3 / 3 | 150k |
| Q scale | `can_rs_025_s*` | reward ×0.25 | 1–3 / 3 | 100k |
| Q scale | `can_rs_2_s*` | reward ×2 | 1–3 / 3 | 100k |
| Q target | `can_hardq_s*` | critic entropy bonus 제거 | 1–3 / 3 | 100k |
| 신규 critic | `can_td_s*` | TD critic | 1–3 / 3 | 150k |
| 신규 critic | `can_cql_s*` | CQL critic | 1–3 / 3 | 150k |
| 신규 critic | `can_calql_s*` | Cal-QL critic | 1–3 / 3 | 150k |
| 신규 교차 | `can_calql_t12i_s*` | Cal-QL + 목표 12 + 초기 α=0.3 | 1–3 / 3 | 150k |
| 신규 교차 | `can_calql_prefill_s*` | Cal-QL + prefill | 1–3 / 3 | 150k |

### Square: 36 run

| 축 | `exp_id` 패턴 | 조건 | seed / n | 예산 |
|---|---|---|---:|---:|
| 기본 | `square_baseline_s*` | 무작위 critic | 1–5 / 5 | 150k |
| critic | `square_iql_s*` | IQL critic | 1–3 / 3 | 150k |
| replay | `square_mix_prefill_s*` | 데모 prefill | 1–3 / 3 | 150k |
| α | `square_fixalpha_03_s*` | 고정 α=0.3 | 1–5 / 5 | 150k |
| entropy | `square_tent12_s*` | auto-α, 목표 12 | 1–5 / 5 | 150k |
| entropy | `square_tent6_s*` | auto-α, 목표 6 | 1–3 / 3 | 150k |
| 인과검증 | `square_tent12_hq_s*` | 목표 12 + critic entropy bonus 제거 | 1–3 / 3 | 150k |
| 신규 critic | `square_td_s*` | TD critic | 1–3 / 3 | 150k |
| 신규 critic | `square_cql_s*` | CQL critic | 1–3 / 3 | 150k |
| 신규 critic | `square_calql_s*` | Cal-QL critic | 1–3 / 3 | 150k |

## 3. 오프라인 사전학습 artifact

| 과제 | artifact | seed / 개수 |
|---|---|---:|
| Can | `warmup_can_s*.pt` | 1–3 / 3 |
| Can | `iql_can_s*.pt` | 1–5 / 5 |
| Can | `td_can_s*.pt` | 1–3 / 3 |
| Can | `cql_can_s*.pt` | 1–3 / 3 |
| Can | `calql_can_s*.pt` | 1–3 / 3 |
| Square | `warmup_square_s*.pt` | 1–3 / 3 |
| Square | `iql_square_s*.pt` | 1–3 / 3 |
| Square | `td_square_s*.pt` | 1–3 / 3 |
| Square | `cql_square_s*.pt` | 1–3 / 3 |
| Square | `calql_square_s*.pt` | 1–3 / 3 |

신규 TD/CQL/Cal-QL artifact 18개는 각각 50k offline step 완료. Square 데이터는
79,828행, `returns_gamma=0.9990000128746033`이고 허용오차 `1e-6` 검사 통과.
Square 9개 artifact의 method metadata가 모두 일치하며 offline diagnostic은 0 reject.

## 4. 9/7~9/8 신규 축 구현과 실행 기록

- 핵심 구현 커밋: `5bf75dc` (TD/CQL/Cal-QL 축).
- Drive 전수검사: `abed356` (`scripts/inspect_runs.py`).
- 검사기와 TD recovery 수정: `fe4159d`.
- 실행 전용 노트북: `e3e5c83`.
  - `colab/vm1_new.ipynb`
  - `colab/vm2_new.ipynb`
  - `colab/vm3_new.ipynb`
- Square return gamma 부동소수점 허용오차 수정: `f6a74d3`.
- Hydra 신규 검사 키는 `+check_states=4096`: `ae9ab91`.

중요한 수정/교훈:

1. TD 온라인 run은 반드시 `variant=td`다. 과거 노트북의 `variant=cql` 매핑은 수정됨.
2. Hydra struct에 없는 진단 키는 `check_states=4096`이 아니라
   `+check_states=4096`으로 추가한다.
3. Square의 저장된 float32 gamma는 `0.9990000128746033`; 정확 비교가 아니라
   `abs(got - 0.999) < 1e-6`을 쓴다.
4. 같은 `exp_id`와 같은 설정으로 재실행하면 checkpoint resume. `total_env_steps`는
   fingerprint에서 제외되어 100k→150k 연장을 지원한다.
5. resume가 만든 eval step 중복은 `plot_results.py`가 더 나중 행을 사용한다.
6. `.out`의 `[done]`만 없더라도 `ckpt ...@150000[mrr]`이면 모델, replay buffer, RNG가
   모두 있다. 같은 명령을 다시 실행하면 0-step resume로 clean `[done]`을 남길 수 있다.

실행 중 RAM 때문에 배치를 8/8/8로 재분배했다.

- VM1: Can TD 3 + CQL 3 + Cal-QL s1–2.
- VM2: Can Cal-QL+t12i 3 + Cal-QL+prefill 3 + `can_calql_s3` + `square_calql_s3`.
- VM3: Square TD 3 + CQL 3 + Cal-QL s1–2.

최종 결과는 VM 배치가 아니라 `exp_id` 기준으로 Drive에 합쳐져 있다.

## 5. 현재까지 확정된 연구 결론

신규 TD/CQL/Cal-QL 24개의 최종 집계는 아직 하지 않았다. 아래는 9/7 이전 축과
Square hq 실험까지 확정된 결론이다.

1. DSRL O2O dip의 직접적인 시간축은 SAC auto-α가 목표 엔트로피 0을 향해 정책
   엔트로피를 약 17→0으로 무너뜨리는 초기 15k online-step 구간과 일치한다.
2. critic 사전학습(IQL/warmupc)은 dip을 얕게 하지만 시점과 최종 성능은 크게 못 바꿨다.
3. Can에서는 고정 α=0.3 또는 목표 엔트로피 12 + 초기 α=0.3이 dip을 없앤다.
4. Square에서도 높은 엔트로피는 초기 dip을 없애지만 후반 성능을 잃는다. γ=0.999에서
   critic target의 entropy bonus가 누적되어 Q_W가 크게 부풀어 오르는 현상이 관측됐다.
5. `square_tent12_hq_s{1,2,3}`는 critic target의 entropy bonus를 제거했을 때
   127k 평균 0.49로 baseline n=5 평균 0.40, tent12 n=5 평균 0.37보다 높았다.
   n=3이고 42k가 사전 판정선 경계이므로 포스터에서는 **supports/시사적**으로 표현한다.
6. Q scale 가설과 그 위의 gate 설계는 기각했다. 숨기지 않는다.

표현 주의:

- “α=0.3이 답”이 아니다. Square에서는 틀렸다.
- “인과가 확정됐다”보다 “critic-target entropy bonus 가설을 지지한다”라고 쓴다.
- 개별 seed 한 점이 아니라 mean±SE를 사용한다.
- 선행 연구 최초 주장에는 “to our knowledge”를 붙이고 LP-DS를 반드시 인용한다.

## 6. 다른 컴퓨터에서 바로 이어가기

### 코드

```bash
git clone -b o2o https://github.com/msp0617/dsrl.git
cd dsrl
git log -6 --oneline
```

이미 clone이 있으면:

```bash
git switch o2o
git pull --ff-only origin o2o
```

### 최신 CSV bundle 만들기

현재 Drive의 `csv_bundle.zip`은 9/6판이라 신규 24개가 들어 있지 않을 수 있다.
아무 Colab CPU 런타임에서 Drive만 mount한 뒤 반드시 다시 만든다.

```python
from google.colab import drive
drive.mount('/content/drive')
```

```bash
%%bash
cd /content/drive/MyDrive/dsrl_project
zip -qr csv_bundle.zip logs \
  -i 'logs/*/eval_log.csv' 'logs/*/train_log.csv'
ls -lh csv_bundle.zip
```

`csv_bundle.zip`을 새 컴퓨터로 내려받아 임의 폴더에 푼다. 분석 스크립트에는
압축 해제 후의 `logs` 디렉터리를 넘긴다.

### 신규 축 우선 집계

분석용 Python에는 `numpy`, `pandas`, `matplotlib`만 있으면 된다.

```bash
python scripts/plot_results.py --logs <압축을_푼_경로>/logs --out <출력_경로> \
  --axes "critic_new=baseline,iql,td,cql,calql;calql_can=baseline,calql,calql_t12i,calql_prefill;critic_new_square=square_baseline,square_iql,square_td,square_cql,square_calql;square_causal=square_baseline,square_tent12,square_tent12_hq,square_calql"
```

출력의 `metrics.csv`와 `summary.csv`에서 다음을 확인한다.

- Can/Square 각 TD, CQL, Cal-QL의 step 0, seed-mean minimum, dip depth,
  early AUC, 100k, 129k/127k, mean±SE.
- `can_calql_t12i`와 `can_calql_prefill`이 Cal-QL 단독보다 무엇을 바꾸는지.
- Cal-QL 단독이 dip을 없애는지, IQL처럼 얕게만 하는지.
- CQL의 offline Q-scale 붕괴가 online 성능에도 남는지.

그 뒤 기존 `STATUS_2026-09-07.md`의 표와 포스터 그림/문구를 갱신한다.

## 7. 다음 작업과 중단 기준

### 지금 해야 하는 것

1. 최신 `csv_bundle.zip` 생성 및 다른 컴퓨터로 복사.
2. 신규 24개 포함 `plot_results.py` 실행.
3. 신규 축 mean±SE 표를 만들고 과장 없이 해석.
4. 포스터 그림, 캡션, 숫자, 참고문헌 검증.

### 지금 하지 않는 것

- 신규 학습을 관성적으로 더 띄우지 않는다.
- `square_tent12_hq_s4,s5`는 n=5 정렬에 유용하지만 포스터 필수 실험은 아니다.
- `calql+hq`, `prefill+tent12i`, 목표 엔트로피 12→0 스케줄, LP-DS 비교,
  Lift/Transport와 seed 확장은 제출 뒤 논문 확장 후보로 둔다.
- 신규 24개 결과를 집계하기 전에는 조건부 후속인 `calql+hq`를 시작하지 않는다.

## 8. 로컬 작업 트리 주의

9/8 인수인계 작성 직전 로컬에는 다음 포스터 작업이 있었다.

- `poster/README.md`: `replay_bars.py` 사용법 반영.
- `scripts/replay_bars.py`: replay 축 early-AUC/129k 막대그래프 생성기.

인수인계 커밋에 두 파일을 원문 그대로 포함했다. 다른 컴퓨터에서는 `git pull` 후
`python scripts/replay_bars.py --summary <figs>/summary.csv --out <figs>/replay_bars.png`로 쓴다.
